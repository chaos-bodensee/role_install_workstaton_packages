---

- name: Install common base office packages 
  package:
    name: "{{ office_packages }}"
    state: latest

- name: Install some terminal needs 
  package:
    name: "{{ shell_packages }}"
    state: latest

- name: Install packages for virtualisation
  package:
    name: "{{ virtualisation }}"
    state: latest


